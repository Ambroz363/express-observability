services:
  app:
    build: .
    command: npm start
    ports:
      - "8081:8081"
    environment:
      - NODE_ENV=development
  prometheus:
    image: prom/prometheus
    volumes:
      -  ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      -  "9090:9090"
